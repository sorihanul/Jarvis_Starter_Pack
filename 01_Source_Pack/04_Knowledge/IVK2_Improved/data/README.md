# IVK2 Local Data

이 폴더는 IVK2가 로컬에서 생성하는 검색 인덱스를 두는 위치다.

## 공개 패키지 규칙

- `*.sqlite` 파일은 배포물에 포함하지 않는다.
- 처음 `build`를 실행하면 로컬 환경에 맞는 `index.sqlite`가 생성된다.
- 생성된 DB에는 사용자의 로컬 파일 경로가 들어갈 수 있으므로 Git에 올리지 않는다.


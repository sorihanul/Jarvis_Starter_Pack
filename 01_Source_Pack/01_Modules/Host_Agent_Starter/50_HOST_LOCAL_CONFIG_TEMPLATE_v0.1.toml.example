# Example local host-workspace config for Jarvis Starter users.
# Rename and place as host_runtime_config.toml in your trusted project if needed.
# This file shows one local role-fragment pattern for advanced users.
# The distributable default is standalone custom agents under host_agent_examples/agents/*.toml.
# Treat this file as a local example that may need adjustment for your host workspace.

[features]
multi_agent = true

[agents]
max_threads = 4
max_depth = 1

# Non-standard local example below:
# if your host workspace does not support this pattern, use host_agent_examples/agents/*.toml instead.
[agents.research]
description = "Read docs, inspect structure, and return facts and paths."
config_file = "../01_Modules/Host_Agent_Starter/roles/research.toml"

[agents.implementation]
description = "Make bounded changes with clear file ownership."
config_file = "../01_Modules/Host_Agent_Starter/roles/implementation.toml"

[agents.validation]
description = "Verify tests, behavior, and remaining risks without taking over implementation."
config_file = "../01_Modules/Host_Agent_Starter/roles/validation.toml"

[agents.synthesis]
description = "Combine sub-agent results into a concise final report for the main session."
config_file = "../01_Modules/Host_Agent_Starter/roles/synthesis.toml"

[agents.security_gate]
description = "Check inbound or outbound risk, secrets, and policy-sensitive content."
config_file = "../01_Modules/Host_Agent_Starter/roles/security_gate.toml"

[agents.writing]
description = "Draft or revise text while preserving user tone, structure, and constraints."
config_file = "../01_Modules/Host_Agent_Starter/roles/writing.toml"

# Optional:
# You can keep project instruction discovery standard and let the host workspace read AGENTS.md.
# project_doc_max_bytes = 65536
# project_doc_fallback_filenames = ["TEAM_GUIDE.md"]

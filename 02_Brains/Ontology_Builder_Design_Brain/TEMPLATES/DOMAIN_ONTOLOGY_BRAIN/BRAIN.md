# Brain

## identity

`<Domain>_Ontology_Brain` manages one domain ontology: `<Domain>`.

## owns

```text
domain source ledger
concept candidates
relation candidates
attribute candidates
rules and exceptions
conflict ledger
ontology exports
connector packets
```

## does not own

```text
consumer brain reports
code implementation
general research outside domain
global ontology standards
```

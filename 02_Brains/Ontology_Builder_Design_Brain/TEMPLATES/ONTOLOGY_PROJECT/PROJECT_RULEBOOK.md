# Project Rulebook

## separation rule

```text
INPUTS = raw material
WORK = candidates and unresolved work
ONTOLOGY = promoted domain structure
EXPORTS = consumer-ready packets
INDEX = retrieval and brain-use routes
```

Do not merge these surfaces by default.

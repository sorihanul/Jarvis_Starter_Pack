# Jarvis Starter Pack v2 설치 및 사용법

이 문서는 GitHub에서 `Jarvis Starter Pack v2`를 내려받아 호스트 작업 환경에서 사용하는 절차를 설명합니다.

---

## 1. 자비스 스타터 v2란?

자비스 스타터 v2는 IDE형 AI 환경에서 개인용 브레인을 구성하기 위한 스타터입니다.

코딩 전용 시스템이 아닙니다.
자료 조사, 글쓰기, 문서 정리, 프로젝트 관리처럼 비코딩 작업에서도 사용할 수 있습니다.

기본 브레인은 정보와 설계 담당 비서 브레인입니다.
사용자가 자연어로 요청하면, 필요한 작업에 맞춰 새 브레인이나 프로젝트 작업장 구조를 구성합니다.

---

## 2. 권장 실행 환경

권장 환경은 다음과 같습니다.

- 로컬 폴더를 읽고 파일을 생성할 수 있는 IDE형 AI 환경
- 프로젝트 지침, 역할 파일, 작업 로그를 읽고 쓸 수 있는 호스트 작업 환경
- 웹 대화만이 아니라 작업 폴더를 직접 다룰 수 있는 AI 작업 환경

v2는 폴더를 작업 단위로 열고, 문서 지침을 다시 읽으며, 필요한 파일을 생성할 수 있는 환경에서 가장 안정적으로 작동합니다.

---

## 3. GitHub에서 다운로드

v2는 아래 브랜치에 있습니다.

```text
https://github.com/sorihanul/Jarvis_Starter_Pack/tree/jarvis-starter-pack-v2
```

### ZIP으로 받기

1. 위 브랜치 페이지로 이동합니다.
2. 초록색 `Code` 버튼을 누릅니다.
3. `Download ZIP`을 선택합니다.
4. 내려받은 ZIP 파일의 압축을 풉니다.
5. 압축을 푼 폴더에서 `Jarvis_Starter_Pack_v2` 폴더를 확인합니다.

### Git으로 받기

Git을 사용할 수 있다면 아래 명령으로 v2 브랜치만 받을 수 있습니다.

```bash
git clone --branch jarvis-starter-pack-v2 --single-branch https://github.com/sorihanul/Jarvis_Starter_Pack.git Jarvis_Starter_Pack_v2
```

Git을 모르면 ZIP 방식으로 받으면 됩니다.

---

## 4. 처음 부팅하기

호스트 작업 환경에서 `Jarvis_Starter_Pack_v2` 폴더를 작업 폴더로 지정합니다.

그다음 대화창에 아래처럼 입력합니다.

```text
부팅해.
```

모델은 아래 파일을 기준으로 메인 비서 브레인을 부팅합니다.

```text
BOOT.md
START_HERE.md
MAP.md
00_Orchestrator/Jarvis_Main_Brain/BOOT.md
00_Orchestrator/LOCAL_RULEBOOK.md
00_Orchestrator/MEMORY_MAP.md
00_Orchestrator/SESSION_CARD.md
00_Orchestrator/Jarvis_Main_Brain/BRAIN.md
00_Orchestrator/Jarvis_Main_Brain/AILO_INTENT_LAYER.md
```

사용자는 위 파일을 직접 외울 필요가 없습니다.
처음에는 `부팅해.`만 입력하면 됩니다.

---

## 5. 브레인 제작 요청하기

부팅이 끝나면 담당 비서에게 필요한 브레인을 요청합니다.

예시는 다음과 같습니다.

```text
정보형 브레인 만들어줘.
부족한 건 네가 정리하고, 바로 쓸 수 있게 폴더와 소환문구까지 준비해.
```

```text
글쓰기 브레인 만들어줘.
내가 자료를 모으고 글로 정리하는 용도로 쓸 거야.
필요한 룰북, 메모리맵, 로그 구조까지 만들어줘.
```

```text
프로젝트 작업장 만들어줘.
목표, 로드맵, 작업 기록, 다음 세션 인수인계까지 관리할 수 있게 구성해줘.
```

자비스는 요청을 목적, 범위, 금지, 산출물로 정리한 뒤 새 브레인이나 작업장 구조를 생성합니다.

---

## 6. 제작된 브레인 사용하기

새 브레인이 생성되면 해당 브레인 폴더를 확인합니다.

그다음 호스트 작업 환경에서 제작된 브레인 폴더를 작업 폴더로 지정합니다.

새 대화창에서 다시 아래처럼 입력합니다.

```text
부팅해.
```

그러면 해당 폴더의 브레인, 룰북, 메모리맵, 세션카드, 작업 기록을 기준으로 목적에 맞는 비서를 사용할 수 있습니다.

---

## 7. 옵션팩 방식

자비스 스타터 v2는 모든 기능을 한 번에 읽히지 않습니다.

기본 브레인은 가볍게 부팅하고, 필요한 능력은 옵션팩을 통해 브레인 구조에 반영합니다.

예시는 다음과 같습니다.

- 자료 조사 브레인: `Evidence_Intake_Pack`
- 복잡한 개념 관계 정리 브레인: `Ontology_Pack`
- 장기 프로젝트 작업장: `Project_Workspace_Lite_Starter`
- 긴 작업 맥락 정리: `Context_Compression_Pack`
- 파일 후보와 읽기 순서 정리: `Memory_Access_and_Route_Pack`

옵션팩은 기본 부팅 때 전부 읽히지 않습니다.
요청에 필요한 능력만 선택해 브레인이나 작업장 구조에 반영합니다.

---

## 8. 작업이 이어지는 방식

자비스 스타터는 모델을 학습시키는 도구가 아닙니다.

모델이 다시 읽을 문서 구조를 만들어, 세션이 바뀌어도 작업을 이어가기 쉽게 하는 방식입니다.

작업 기록, 룰북, 로그, 캡슐, 즉 다음 세션용 요약을 폴더 안에 남깁니다.
그래서 폴더를 복사하면 다른 컴퓨터에서도 작업 상태를 이어갈 수 있습니다.

단, 기존 대화창 안의 대화 맥락이 이관되는 것은 아닙니다.
문서로 남은 작업 상태가 이관됩니다.

---

## 9. 장기 사용 시 리프레시

장기간 사용하면 모델이 세부 설정을 누락할 수 있습니다.

그럴 때는 아래 파일을 다시 읽히도록 요청합니다.

```text
브레인, 로컬 룰북, 메모리맵, 세션카드, 로그를 다시 읽고 현재 작업 기준으로 리프레시해줘.
```

호스트 작업 환경은 장기 스레드 운용 중 컨텍스트를 압축해 이어가는 기능이 있어 긴 작업에 비교적 유리합니다.

긴 대화를 계속 누적하면 모델이 앞선 설정을 누락하거나 작업 지시와 다른 방향으로 응답할 수 있습니다.

이 경우 새 스레드를 시작하고 브레인, 룰북, 로그를 다시 읽히는 방식을 권장합니다.

---

## 10. 자주 쓰는 명령문

### 스타터 부팅

```text
부팅해.
```

### 새 브레인 제작

```text
내 목적에 맞는 브레인 하나 만들어줘.
부족한 정보는 네가 정리하고, 바로 쓸 수 있게 폴더와 소환문구까지 준비해.
```

### 제작된 브레인 부팅

```text
이 폴더의 브레인을 기준으로 부팅해.
START_HERE.md와 BOOT.md를 먼저 읽고, 현재 작업 기록 위치를 확인해줘.
```

### 장기 세션 리프레시

```text
브레인, 로컬 룰북, 메모리맵, 세션카드, 로그를 다시 읽고 현재 작업 기준으로 리프레시해줘.
```

---

## 11. 한 줄 요약

GitHub의 `jarvis-starter-pack-v2` 브랜치에서 ZIP을 내려받고, 호스트 작업 환경에서 폴더를 지정한 뒤 `부팅해.`를 입력하면 자비스 스타터 v2를 사용할 수 있습니다.
